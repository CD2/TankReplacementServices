= form_for([:admin, @page]) do |f|

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :banner
    = f.file_field :banner
  .field
    = f.label :body
    = f.cktext_area :body
  .field
    = f.submit 'Save'